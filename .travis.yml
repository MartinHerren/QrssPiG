dist: trusty
sudo: false

language: cpp

compiler:
  - clang
  - gcc

addons:
  apt:
    packages:
      - libboost-program-options-dev
      - libyaml-cpp-dev
      - libfftw3-dev
      - libgd2-noxpm-dev
      - libfreetype6-dev
      - libssh-dev
      - libcurl4-openssl-dev
      - librtfilter-dev

script:
  - mkdir -p build && cd build && cmake .. && cmake --build .
