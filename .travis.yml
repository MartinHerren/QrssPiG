dist: trusty
sudo: required

language: cpp

compiler:
  - clang
  - gcc

addons:
  apt:
    packages:
      - libboost-program-options-dev
      - libgd2-noxpm-dev
      - libssh-dev
      - libfftw3-dev
      - libyaml-cpp-dev
      - libfreetype6-dev
      - librtfilter-dev
      - libusb-1.0-0-dev
      - libhackrf-dev

before_install:
  - ./install-libhackrf.sh

script:
  - mkdir -p build && cd build && cmake .. && cmake --build .
